---
title: "KO_TIL_NK_process"
output: html_notebook
---

```{r setup}
library(tidyverse)
library(Seurat)
library(scmisc)
library(cowplot)
library(patchwork)
library(ggplot2)
set.seed(1)
```

# load the data 

```{r}
m <- Read10X_h5("~/KO_NK_filtered_feature_bc_matrix.h5")
```


```{r}
colnames(m) <- paste0("KO:", colnames(m))
```

```{r}
x <- CreateSeuratObject(counts = m, project = "NK_TIL")
```

```{r}
x$background <- "KO_TIL"
```

```{r}
x <- NormalizeData(x)
```

```{r}
x <- FindVariableFeatures(x, nfeatures = 5000) 
VariableFeaturePlot(x)
x <- ScaleData(x)
x <-  RunPCA(x)
```

```{r} 
ElbowPlot(x, ndims = 50)
```

```{r}
DimPlot(x)
```

```{r}
x<- RunUMAP(x, dims = 1:20)
```
 
```{r}
x <- FindNeighbors(x, dims = 1:20)
```

```{r message=TRUE, warning=FALSE}
x <- FindClusters(x, algorithm = 4, resolution = 0.6)
```

```{r}
DimPlot(x, label = TRUE) + NoLegend()
```

# mito 

```{r}
x <- PercentageFeatureSet(x, "mt-", col.name = "mito.percentage")
```

```{r, fig.width=8, fig.height=6}
VlnPlot(x, c("nCount_RNA", "nFeature_RNA","mito.percentage"),ncol = 1, pt.size=0.1)
```

```{r, fig.width=8}
mtgene <- grep("mt-",rownames(x), value = TRUE)

DoHeatmap(x, mtgene, slot = "data") +
  scale_fill_gradient(low = "white", high = "red") +
  guides(color = FALSE)
```


```{r, fig.width=8}
genes <- grep("^Rpl[0-9][0-9]$", rownames(x), value = TRUE)
DoHeatmap(x, genes, slot = "data") +
  scale_fill_gradient(low = "white", high = "red") +
  guides(color = FALSE)

genes <- grep("^Rps[0-9][0-9]$", rownames(x), value = TRUE)
DoHeatmap(x, genes, slot = "data") +
  scale_fill_gradient(low = "white", high = "red") +
  guides(color = FALSE)
```

# cell cycle

```{r}
cc <- sapply(cc.genes.updated.2019, str_to_sentence)
x <- CellCycleScoring(x, cc$s.genes, cc$g2m.genes)
```

```{r fig.width=10, fig.height=3}
FeaturePlot(x, c("S.Score", "G2M.Score"), order = TRUE, blend = TRUE, label=TRUE)
```

# markers 

## tumor marker 

```{r}
tumor <- c("Dct","Pmel","Tyrp1","Mlana")
```

```{r, fig.width=6, fig.height=3}
VlnPlot(x, features = tumor, ncol=2)
DotPlot(x, features = tumor, dot.scale = 6)+ RotatedAxis()+
  scale_colour_distiller(palette = "RdBu")
```

# Isolated NK cells

```{r}
z <- x[, !x$seurat_clusters %in% c("6","7","12")] # cluster 6: dying cell; 7: T cell: 12: tumor
DimPlot(z)
```

# Export 

```{r}
write_rds(z, "~/KO_TIL_NKonly_process.rds")
```

# sessionInfo

```{r}
sessionInfo()
```















